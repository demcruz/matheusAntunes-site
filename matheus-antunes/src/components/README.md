# Components

Atomic design structure:

- atoms: UI primitives (Button, Icon)
- molecules: small combinations (InputField, CardHeader)
- organisms: page sections (Header, Footer, Hero)
- templates: page-level assemblies used by routes (ShapeSobControle)

Naming:
- folders: lowercase
- components: PascalCase filenames matching the component name
